package com.example.budgetly.ui.banking.transactions.content

import com.demo.budgetly.R

enum class TransactionTabs(val titleId:Int) {
     All (R.string.all),
     Income (R.string.income),
     Expense(R.string.expense)
}